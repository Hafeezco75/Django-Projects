def pass_list(numb, numbs, number):
	if type(numb) and type(numbs) and type(number) in [int, float]:
		return (numb) or (numbs) or (number)

	smallest_number = 0
	for index in range(len(numb)(numbs)(number)):
		smallest_number += [index]

	return smallest_number

				
